name: cremen_eatstreet_shop_application
description: "Cremenkart — order authentic Surat street food for pickup or delivery."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_bloc: ^8.1.3
  google_fonts: ^6.2.1
  intl: ^0.20.2
  equatable: ^2.0.7
  go_router: ^14.8.0
  url_launcher: ^6.3.1
  flutter_animate: ^4.5.0
  hive: ^2.2.3
  hive_flutter: ^1.0.0
  path_provider: ^2.0.0
  supabase_flutter: ^2.8.0
  freezed_annotation: ^3.0.0
  dio: ^5.7.0
  razorpay_flutter: ^1.4.0
  geolocator: ^14.0.3
  google_mobile_ads: ^9.1.0
  get_it: ^9.2.1
  injectable: ^3.0.0
  animations: ^2.0.11
  shimmer: ^3.0.0
  cached_network_image: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  build_runner: ^2.4.13
  freezed: ^3.0.0
  bloc_test: ^9.1.7
  mocktail: ^1.0.4
  shared_preferences: ^2.5.5
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.8
  injectable_generator: ^3.0.2
  image: ^4.3.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# `dart run flutter_launcher_icons` regenerates the launcher icon on every
# platform from this one source image (the real Cremen Eat Streets logo,
# already 1024x1024). `image_path` (legacy square icon) still backs iOS,
# Windows, and pre-26 Android — but Android 8+ launchers mask a square icon
# into a circle/squircle, which crops this logo's brand ring since it runs
# edge-to-edge with no safe-zone padding. `adaptive_icon_foreground` is a
# separately generated version of the same logo scaled to ~66% and centered
# on a transparent canvas (see tool/generate_adaptive_icon.dart) so Android's
# own mask has margin to crop into instead of cutting into the logo itself.
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/cremen_logo.jpg"
  min_sdk_android: 21
  remove_alpha_ios: true
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/images/cremen_logo_adaptive_fg.png"
  windows:
    generate: true
    image_path: "assets/images/cremen_logo.jpg"
    icon_size: 256

# `dart run flutter_native_splash:create` regenerates the OS-level splash
# shown before the Flutter engine paints its first frame — this app's own
# animated Splash screen (lib/features/splash) takes over immediately after.
# Single white background (not a light/dark pair) because the logo's own
# background is baked-in solid white, not transparent; splitting light/dark
# would show a visible white box around the logo in dark mode.
flutter_native_splash:
  color: "#FFFFFF"
  image: assets/images/cremen_logo.jpg
  android_12:
    image: assets/images/cremen_logo.jpg
    color: "#FFFFFF"
  web: false
